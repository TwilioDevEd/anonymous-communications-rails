<%= form_for(@vacation_property) do |f| %>
  <% if @vacation_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vacation_property.errors.count, "error") %> prohibited this vacation_property from being saved:</h2>

      <ul>
      <% @vacation_property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
